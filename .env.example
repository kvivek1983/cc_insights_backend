# === Database ===
# App connects to localhost:{DB_PORT} — the SSH tunnel maps this to the private DB
DB_HOST=127.0.0.1
DB_PORT=15432
DB_NAME=cleancheck
DB_USER=cleancheck_read_only
DB_PASSWORD=6E8LQ0m`/HhA

# === SSH Tunnel (Railway deployment) ===
# Paste the FULL contents of keyp_live_sshtunnel.pem as a single env var
# SSH_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----
JUMP_HOST=13.234.98.188
JUMP_USER=ubuntu
DB_PRIVATE_HOST=172.17.5.229

# === App ===
DEFAULT_COMPANY_ID=15
CORS_ORIGINS=http://localhost:3000,https://onecheck-dashboard.vercel.app
